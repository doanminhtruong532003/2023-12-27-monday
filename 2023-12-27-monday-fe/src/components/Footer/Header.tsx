import React from "react";

const Header: React.FC = () => {
  return (
    <header className="bg-blue-100 shadow-md p-4 flex justify-between items-center">
      {/* Các thành phần của Header ở đây */}
      Header đây nè
    </header>
  );
};

export default React.memo(Header);
